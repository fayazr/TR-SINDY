# Core (required)
PyQt6>=6.0.0
opencv-python>=4.5.0
opencv-contrib-python>=4.5.0   # needed for TV-L1 optical flow (cv2.optflow)
numpy>=1.19.0
scipy>=1.7.0
pandas>=1.3.0
matplotlib>=3.3.0
pysindy>=1.7.0

# Scientific export formats (optional but recommended)
h5py>=3.0.0
pyarrow>=8.0.0

# Advanced interpolation (optional)
pykrige>=1.6.0

# Machine-learning features (optional - enables PINN, RAFT, FNO, VAE, GAN, etc.)
# Install CPU or CUDA wheels as appropriate for your platform.
torch>=2.0.0
torchvision>=0.15.0

# Trained-model export to ONNX (optional - enables the ONNX format in the ML tab)
onnx>=1.14.0
